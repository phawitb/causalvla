[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "causal_aug"
version = "0.1.0"
description = "GPU-based counterfactual augmentation for CausalVLA"
requires-python = ">=3.10"
dependencies = ["torch>=2.0"]
